<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>
    <div id='main_wrapper'>
      <table>
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Email</th>
        </tr>
        <% @users.each do |u| %>
          <tr>
            <td><%= u.first_name %></td>
            <td><%= u.last_name %></td>
            <td><%= u.email %></td>
            <td>
              <a href='/users/<%= u.id %>'>Show</a>
              <a href='/users/<%= u.id %>/edit'>Edit</a>
              <a href='/users/<%= u.id %>/destroy'>Destroy</a>
            </td>
          </tr>
        <% end %>
      </table>
      <a href='/users/new'>New User</a>
    </div>
</body>


</html>
